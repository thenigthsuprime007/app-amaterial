import { Routes } from '@angular/router';
import { EjemplosAmaterialComponent } from './ejemplos-amaterial/ejemplos-amaterial.component';

export const routes: Routes = [
    {path: "ejemplos", component:EjemplosAmaterialComponent}
];
